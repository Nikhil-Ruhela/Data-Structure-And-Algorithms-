#include<iostream>
#include<cmath>
using namespace std;

int main(){
    double n, m; 
    cout<<"Enter the number n"<<" ";
    cin>>n;
    cout<<"Enter the number m"<<" ";
    cin>>m;
    
    cout<<"The squar root of given no. is"<<" ";
    cout<<sqrt(n);
    cout<<endl;  
    cout<<"The maximum of given no. is"<<" ";
    cout<<max(n,m);

    return 0;
}